{
  "name": "molle-rc",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=14.15.0"
  },
  "scripts": {
    "prepublish": "yarn test",
    "publish": "yarn lerna publish"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.2.1"
  }
}
